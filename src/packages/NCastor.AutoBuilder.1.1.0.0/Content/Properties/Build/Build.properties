<?xml version="1.0" encoding="utf-8"?>

<!--
"Your stuff"

Your custom Build settings
-->

<Project DefaultTargets="All" xmlns="http://schemas.microsoft.com/developer/msbuild/2003">

  <!-- TODO: Place your custom Build settings here -->

  <PropertyGroup>

    <!--Uncomment to modify the Build command line arguments for the Special projects-->
    <!--<SpecialProjectsBuildProperties>
      Configuration=$(Configuration);
      Platform=$(Platform);
      $(SpecialProjectsBuildProperties);
    </SpecialProjectsBuildProperties>-->

    <!--Uncomment to modify the Build command line arguments used to build the solution-->
    <!--<BuildProperties>
      Configuration=$(Configuration);
      Platform=$(Platform);
      OutputPath=$(BuildingPath);
      $(BuildProperties);
    </BuildProperties>-->

  </PropertyGroup>

</Project>
